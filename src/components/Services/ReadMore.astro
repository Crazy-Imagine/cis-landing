---
import { getLangFromUrl, useTranslations } from '@/i18n/utils';
import Typography from '@/components/Typography.astro';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<Typography
  as="span"
  variant="h6"
  id={Astro.props.id}
  class="cursor-pointer rounded-lg px-2.5 text-center !font-nexaBold uppercase tracking-widest text-blue-purple transition hover:bg-[#f0f0f0]"
>
  {t('common.read-more')} »
</Typography>
